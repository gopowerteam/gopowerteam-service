export enum SortMode {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum WhereOperator {
  In = 'IN',
  Like = 'LIKE',
  Equal = 'EQUAL',
  Between = 'BETWEEN',
}

export enum PaginatorMode {
  Cursor = 'CURSOR',
  Index = 'INDEX',
}
